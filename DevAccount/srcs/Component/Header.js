import React from 'react'
import { Text } from 'react-native'

import * as styled from '../Styles/Header'

function Header () {
	return (
		<styled.Header>
			<styled.Text>My Account</styled.Text>
		</styled.Header>
	)
}

export default Header;